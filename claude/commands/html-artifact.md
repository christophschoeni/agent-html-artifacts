Use the html-artifact skill to create a self-contained HTML artifact for the user's request.

Rules:

- Keep durable source in Markdown, JSON, or YAML.
- Use HTML for long human-facing plans, reviews, research briefs, module maps, reports, and throwaway editors.
- Include export buttons when the user needs to feed results back into Claude or another coding agent.
- Avoid external dependencies unless explicitly requested.

User request:

$ARGUMENTS
